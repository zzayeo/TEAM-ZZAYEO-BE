const MESSAGE = {
    NOT_CONNECT_SOCKET_IO: 'socket.io is not initalized',
};

const SOCKET_CORS = {
    origin: '*',
    methods: ['GET', 'POST'],
};

const ROUTE = {
    INDEX: '/',
};

module.exports = {
    SOCKET_CORS,
    MESSAGE,
    ROUTE,
};
